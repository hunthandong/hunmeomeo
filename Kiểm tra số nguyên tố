#include "stdio.h" 
int kiemtra_ngto(int n) {
	if(n<=1) return 0;
	else
	for(int i=2;i<n;i++){
		if(n%i==0) return 0; 
	}
	return 1; 
}
int main() {
	int n;
	scanf("%d",&n);
	if(kiemtra_ngto(n))printf("1");
	else
	     printf("0");
    return 0; 
}
