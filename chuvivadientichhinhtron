#include <stdio.h>
int main() {
    float r,PI=3.14, chuvi=0, dientich=0;
   // printf("Nhap ban kinh r: ");
    scanf("%f", &r);
    dientich = PI *r * r;
    chuvi= PI*r*2; 
    printf(" %.2f\n", chuvi);
    printf(" %.2f\n", dientich);

    return 0;
}
